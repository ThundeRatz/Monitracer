# New MoniTracer

Aplicativo desenvolvido para controle de constantes, manuseio e testagem do robô seguidor de linha Tracer.

Será desenvolvido em React-Native...

## 📱 Geração de versão de release

Para gerar a versão de release basta rodar:

```bash
npm run android-release
```

Esse comando irá abrir no simulador uma versão release do aplicativo e o arquivo `.apk` estará disponível na pasta `android/app/build/outputs/apk/release`. Esse arquivo pode ser usado no App center para gerar uma versão do app.
